# Base URL of the API.
# Native dev → the server on :5000. The Docker stack builds the client with /api
# (nginx reverse-proxies to the server), so you don't need this file there.
REACT_APP_API_BASE_URL=http://localhost:5000/api
